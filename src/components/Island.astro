---
const { hideShadow, noPadding, bottomMargin } = Astro.props;
---

<div
    class:list={[
        "island",
        { "with-shadow": !hideShadow },
        { "with-padding": !noPadding },
        { "bottom-margin": bottomMargin}
    ]}
>
    <slot />
</div>

<style>
    .island {
        display: flex;
        border: 1px solid var(--color-foreground);

        &.with-padding {
            padding: 0.5rem;
        }

        &.bottom-margin {
            margin-bottom: 3rem;
        }
    }
</style>
